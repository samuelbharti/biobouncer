name: R-CMD-check

on:
  push:
    branches: [main]
    paths: ["r/**", "shared/**", ".github/workflows/r.yml"]
  pull_request:
    paths: ["r/**", "shared/**", ".github/workflows/r.yml"]

permissions:
  contents: read

jobs:
  R-CMD-check:
    # Runs once the R package exists. Until then the job is skipped, which keeps
    # PRs that only touch shared/ green.
    if: hashFiles('r/DESCRIPTION') != ''
    runs-on: ${{ matrix.config.os }}
    name: R-CMD-check (${{ matrix.config.os }})
    strategy:
      fail-fast: false
      matrix:
        config:
          - { os: ubuntu-latest, r: "release" }
          - { os: windows-latest, r: "release" }
          - { os: macos-latest, r: "release" }
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
    steps:
      - uses: actions/checkout@v6

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck
          needs: check
          working-directory: r

      - uses: r-lib/actions/check-r-package@v2
        with:
          working-directory: r
          upload-snapshots: true

  lint-r:
    if: hashFiles('r/DESCRIPTION') != ''
    runs-on: ubuntu-latest
    name: lint-r
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v6

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::lintr, local::.
          needs: lint
          working-directory: r

      - name: Lint
        run: lintr::lint_package()
        shell: Rscript {0}
        working-directory: r
        env:
          LINTR_ERROR_ON_LINT: true
