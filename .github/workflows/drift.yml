name: drift

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  drift:
    name: drift
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      # One gate for the shared specs, whichever package a change targets.
      - name: Validate the shared source specs
        run: pip install pyyaml && python tools/check_sources.py
      - name: Sync shared spec into packages
        run: python tools/sync_shared.py
      - name: Fail if vendored copies drift from shared/
        run: git diff --exit-code
