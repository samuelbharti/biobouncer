% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapters.R
\name{sv_biobouncer}
\alias{sv_biobouncer}
\title{Create a shinyvalidate rule}
\usage{
sv_biobouncer(
  source_db,
  how = "pattern",
  species = NULL,
  version = NULL,
  message = NULL
)
}
\arguments{
\item{source_db}{Source key, for example \code{"mondo"}. See \code{\link[=sources]{sources()}}.}

\item{how}{Checking mode: \code{"pattern"} (offline, shape only), \code{"cache"}
(offline existence against a snapshot), \code{"remote"} (live existence against
the source API), or \code{"existence"} (cache when a snapshot is available for
\code{version}, otherwise remote, or pattern for a source with no resolver).}

\item{species}{Optional species context, echoed in the result. A name such as
\code{"homo_sapiens"} or an NCBI taxon id such as \code{9606}. When given, an id of a
different species is invalid: Ensembl is checked from its id prefix (in
\code{pattern} and \code{remote} modes), and UniProt from the entry's organism in
\code{remote} mode. A species outside the source map is not checked.}

\item{version}{Snapshot version. In \code{cache} mode it selects the snapshot and
defaults to the latest installed one when omitted; it selects the snapshot
for \code{existence} mode; ignored in \code{pattern} and \code{remote} modes.}

\item{message}{Optional custom message for an invalid input.}
}
\value{
A function of one argument suitable for \code{add_rule()}.
}
\description{
Returns a rule function for use with \code{shinyvalidate::InputValidator}'s
\code{add_rule()}. The rule returns \code{NULL} for a valid input and a message
otherwise. This adapter does not depend on shinyvalidate; it only produces a
rule the package can consume.
}
\examples{
rule <- sv_biobouncer("mondo")
rule("MONDO:0005148")
rule("mondo:5148")
}
\seealso{
\code{\link[=check_valid_id]{check_valid_id()}}.
}
