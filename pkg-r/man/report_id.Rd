% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{report_id}
\alias{report_id}
\alias{summary.biogate_report}
\alias{print.biogate_report}
\title{Validate and report on a column of identifiers}
\usage{
report_id(
  x,
  source_db,
  how = "pattern",
  species = NULL,
  version = NULL,
  refresh = FALSE,
  on_error = "raise"
)

\method{summary}{biogate_report}(object, ...)

\method{print}{biogate_report}(x, ...)
}
\arguments{
\item{x}{A \code{biogate_report}, as returned by \code{\link[=report_id]{report_id()}}.}

\item{source_db}{Source key, for example \code{"mondo"}. See \code{\link[=sources]{sources()}}.}

\item{how}{Checking mode: \code{"pattern"} (offline, shape only), \code{"cache"}
(offline existence against a snapshot), \code{"remote"} (live existence against
the source API), or \code{"existence"} (cache when a snapshot is available for
\code{version}, otherwise remote, or pattern for a source with no resolver).}

\item{species}{Optional species context, echoed in the result. A name such as
\code{"homo_sapiens"} or an NCBI taxon id such as \code{9606}. When given, an id of a
different species is invalid: Ensembl is checked from its id prefix (in
\code{pattern} and \code{remote} modes), and UniProt from the entry's organism in
\code{remote} mode. A species outside the source map is not checked.}

\item{version}{Snapshot version. In \code{cache} mode it selects the snapshot and
defaults to the latest installed one when omitted; it selects the snapshot
for \code{existence} mode; ignored in \code{pattern} and \code{remote} modes.}

\item{refresh}{In remote checks, skip any cached response and refetch. Ignored
by the offline modes.}

\item{on_error}{How a per-id remote failure is handled. \code{"raise"} (the
default) lets the failure unwind the whole call. \code{"indeterminate"} leaves
just that id \code{NA} with the reason in its \code{error} column and checks the rest
of the batch, so one unreachable id does not lose the others. Ignored by the
offline modes.}

\item{object}{A \code{biogate_report}, as returned by \code{\link[=report_id]{report_id()}}.}

\item{...}{Ignored.}
}
\value{
A \link[tibble:tibble]{tibble}, as returned by \code{\link[=check_id]{check_id()}}, with the extra
class \code{biogate_report}. Calling \code{summary()} on it returns a one-row tibble of
counts.
}
\description{
\code{report_id()} runs \code{\link[=check_id]{check_id()}} over a column and returns its result table
with the extra class \code{biogate_report}, so it prints with a one-line summary of
how many values are valid, repairable, invalid, or missing. It is the
recommended entry point for inspecting and cleaning a column. Use \code{\link[=repair_id]{repair_id()}}
inside \code{dplyr::mutate()} to substitute the fixable values. For enforcing
validity inside a framework, reach for the adapters such as \code{\link[=id_predicate]{id_predicate()}}.
}
\examples{
report_id(c("MONDO:0005148", "mondo:5148"), "mondo")
}
\seealso{
\code{\link[=repair_id]{repair_id()}}, \code{\link[=check_id]{check_id()}}.
}
