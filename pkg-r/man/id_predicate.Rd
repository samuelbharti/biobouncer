% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapters.R
\name{id_predicate}
\alias{id_predicate}
\title{Build a vectorized predicate for data-frame validation}
\usage{
id_predicate(source_db, how = "pattern", species = NULL, version = NULL)
}
\arguments{
\item{source_db}{Source key, for example \code{"mondo"}. See \code{\link[=sources]{sources()}}.}

\item{how}{Checking mode: \code{"pattern"} (offline, shape only), \code{"cache"}
(offline existence against a snapshot), \code{"remote"} (live existence against
the source API), or \code{"existence"} (cache when a snapshot is available for
\code{version}, otherwise remote).}

\item{species}{Optional species context, echoed in the result. A name such as
\code{"homo_sapiens"} or an NCBI taxon id such as \code{9606}. When given, an id of a
different species is invalid: Ensembl is checked from its id prefix (in
\code{pattern} and \code{remote} modes), and UniProt from the entry's organism in
\code{remote} mode. A species outside the source map is not checked.}

\item{version}{Snapshot version. Required for \code{cache} mode; selects the
snapshot for \code{existence} mode; ignored in \code{pattern} and \code{remote} modes.}
}
\value{
A function of one argument that returns a logical vector the same
length as its input.
}
\description{
Returns a function of one argument that reports, element by element, whether
each value is a valid identifier for \code{source_db}. The predicate is the shape
that data-frame validation packages expect, so it drops straight into
\code{assertr::assert()} or a \code{validate::validator()} rule, or into base \code{Filter()}
and \code{vapply()}. It is the R counterpart of the pandera check in the Python
package.
}
\examples{
is_mondo <- id_predicate("mondo")
ids <- c("MONDO:0005148", "mondo:5148", "MONDO:0018076")
is_mondo(ids)
ids[is_mondo(ids)]

# With assertr, if it is installed:
# library(assertr)
# df |> assert(id_predicate("mondo"), term)

# With validate, if it is installed:
# rules <- validate::validator(is_mondo := id_predicate("mondo")(term))
# validate::confront(df, rules)
}
\seealso{
\code{\link[=is_valid_id]{is_valid_id()}}, \code{\link[=check_valid_id]{check_valid_id()}}.
}
